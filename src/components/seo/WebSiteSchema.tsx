// This component is now deprecated - WebSite schema is consolidated in PageLayout.tsx
// Keeping as empty export for backward compatibility
const WebSiteSchema = () => null;
export default WebSiteSchema;
